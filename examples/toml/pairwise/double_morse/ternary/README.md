# Double Morse Ternary Example

This folder contains the ternary double-Morse training example.

- `initial.toml`
- `forgeff.train.toml`

The training setting uses the ternary ethanol dataset under `../../../data/ternary/`.
