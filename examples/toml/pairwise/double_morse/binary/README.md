# Double Morse Binary Example

This folder contains the binary double-Morse training example.

- `initial.toml`
- `forgeff.train.toml`

The training setting uses the binary dataset under `../../../data/binary/`.
