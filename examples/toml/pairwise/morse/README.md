# Morse Pairwise Example

This folder contains a minimal built-in Morse pair potential:

- `initial.toml`
- `forgeff.train.toml`

The `initial.toml` file holds the analytical pair form and the starting
parameters. The training setting points to `../../data/unary/training.cfg`,
so you can replace that path with your own configuration list before running a
real fit.
