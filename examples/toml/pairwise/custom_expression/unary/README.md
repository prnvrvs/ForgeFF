# Custom Expression Unary Example

This folder contains the unary analytical pair example written as a custom
expression.

- `initial.toml`
- `forgeff.train.toml`

The training setting uses the unary dataset under `../../../data/unary/`.
