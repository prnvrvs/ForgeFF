# Custom Expression Binary Example

This folder contains the binary analytical pair example written as a custom
expression.

- `initial.toml`
- `forgeff.train.toml`

The training setting uses the binary dataset under `../../../data/binary/`.
